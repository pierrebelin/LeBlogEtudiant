imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }
    - { resource: ../../src/Pierre/BlogBundle/Resources/config/config.yml }
    - { resource: ../../src/Pierre/BonsPlansBundle/Resources/config/config.yml }
    - { resource: "@PierreBonsPlansBundle/Resources/config/services.yml" }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: fr

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    #serializer:      { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # http://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id:  session.handler.native_file
        save_path:   "%kernel.root_dir%/../var/sessions/%kernel.environment%"
    fragments:       ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# SendinBlue
sendin_blue_api:
    api_key: SF7UngL8As9p6cha
    timeout: 5000

# SEO Bundle
sonata_seo:
    page:
        title: "Le Blog Étudiant"
        metas:
            http-equiv:
                X-UA-Compatible: IE=edge
                Content-Type: text/html
            name:
                viewport: width=device-width, initial-scale=1
#                keywords: default, sonata, seo
            property:
                'og:site_name': Le Blog Étudiant
                'og:title': Le Blog Étudiant
                'og:type': article
                'og:description': Tu es étudiant(e) ? Découvre toutes les aides, avantages, bons plans et astuces de ta ville avec Le Blog Étudiant.
                'og:url': https://le-blog-etudiant.fr
                'og:image': https://pbs.twimg.com/profile_images/864534782071390209/iBXWAKcU_400x400.jpg
                'og:image:secure_url': https://pbs.twimg.com/profile_images/864534782071390209/iBXWAKcU_400x400.jpg
                'fb:app_id': "107059596483790"



# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/../var/data/data.sqlite"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

